/*const image = {
    "src:string": "images/localStorage.png",
    "coords:number[]": [250, 150, 100],
    "alignment:string": "center"

};
*/

function display(id: number, name: string): void {
    console.log("Id = " + id + ", Name = " + name);
}

let employee: {
    id: number,
    name: string
} = {
    id: 100,
    name: "John"
}



Student.student("john", "smith");
Student.getAverageGrade();
Student.enterGrade(90);
Student.enterGrade(80);
Student.enterGrade(100);
Student.getAverageGrade();
console.log(Student.getStudentInfo());

Student.student("jon", "sh");
Student.getAverageGrade();
Student.enterGrade(10);
Student.enterGrade(70);
Student.enterGrade(60);
Student.getAverageGrade();
console.log(Student.getStudentInfo());

using the module pattern write a javacript defination using private and public field.





